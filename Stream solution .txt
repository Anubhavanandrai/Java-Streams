import java.util.*;
import java.util.stream.*;
public class Main
{
	public static void main(String[] args) {
		
	
		String[] arr= {"Man", "Anu", "Ram", "Subham"};

        List<String> stri = Arrays.asList(arr);
                     stri.stream()
                         .map(a->a.length())
                         .collect(Collectors.toList());
    
       System.out.println(stri); 
      
                         
	}
}